import React, { PureComponent } from "react";
import FontMock from "../../components/AuthorizationComponents/FontMock";

export default class Strategy extends PureComponent {
  render() {
    return <FontMock>Strategy</FontMock>;
  }
}
