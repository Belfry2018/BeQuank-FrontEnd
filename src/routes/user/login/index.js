import React, { PureComponent } from "react";
import FontMock from "../../../components/AuthorizationComponents/FontMock";

export default class Login extends PureComponent {
    render() {
        return <FontMock>Login</FontMock>;
    }
}
