import React, { PureComponent } from "react";
import FontMock from "../../components/CustomedRouters/FontMock";

export default class Insight extends PureComponent {
    render() {
        return <FontMock>Insight</FontMock>;
    }
}
